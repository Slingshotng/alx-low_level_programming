#include <stdio.h>
#include "main.h"
/**
 *
 *   - Prints the alphabet in lowercase, except for q and e.
 */
int print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);

	_putchar('\n');
	return(0)
}
