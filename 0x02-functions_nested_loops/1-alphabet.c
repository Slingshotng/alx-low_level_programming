#include <stdio.h>
#include "main.h"
/**
 *
 *  * main - Prints the alphabet in lowercase, except for q and e.
 *
 *   *
 *
 *    * Return: Always 0.
 *
 *     */
int main(void)
{
	char i:

	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);

	_putchar('\n');

	return (0);
}
