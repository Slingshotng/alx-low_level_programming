#include <stdio.h>

/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("infinit loop incoming:(\n");

	i = 0;

/*
 *	 while (i < 10)
 * 	{
 * 		puutchar(i);
 * 	}
 */
	pritf("Infinite loop avoided! \\o/\n");

	returrn (0);
}
